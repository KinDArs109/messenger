-- Длительность голосового сообщения, секунды.
--
-- Хранится, а не считается на месте: Chrome записывает webm без
-- длительности в заголовке, и проигрыватель показывает «бесконечность»,
-- пока не домотает до конца. Клиент знает её в момент записи.
ALTER TABLE "Attachment" ADD COLUMN "duration" INTEGER;
