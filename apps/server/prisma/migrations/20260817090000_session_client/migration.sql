-- Чем зашли: приложение или браузер.
--
-- Из user-agent это выводится не всегда: приложение на Android
-- показывает ровно ту же строку, что и вкладка Chrome. Поэтому клиент
-- сообщает о себе заголовком, а сюда попадает одно из трёх известных
-- слов — или NULL, если вход был раньше, чем появилось поле.
ALTER TABLE "RefreshToken" ADD COLUMN "client" TEXT;
